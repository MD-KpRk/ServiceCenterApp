<UserControl x:Class="ServiceCenterApp.Controls.ClientCard"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:vm="clr-namespace:ServiceCenterApp.ViewModels"
             mc:Ignorable="d" 
             d:DataContext="{d:DesignInstance vm:ClientListItemViewModel}"
             d:DesignHeight="100" d:DesignWidth="450">

    <UserControl.Resources>
        <Style x:Key="TransparentButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
            <Setter Property="VerticalContentAlignment" Value="Stretch"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <ContentPresenter/>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </UserControl.Resources>

    <Button Command="{Binding DataContext.SelectClientCommand, RelativeSource={RelativeSource AncestorType=Page}}"
            CommandParameter="{Binding}"
            Style="{StaticResource TransparentButtonStyle}">

        <Border Background="White" CornerRadius="5" Margin="5"
                BorderThickness="1" BorderBrush="#E0E0E0" 
                Width="400" Height="76">

            <Border.Style>
                <Style TargetType="Border">
                    <Style.Triggers>
                        <DataTrigger Binding="{Binding IsMouseOver, RelativeSource={RelativeSource AncestorType=Button}}" Value="True">
                            <Setter Property="BorderBrush" Value="{StaticResource PrimaryBrush}"/>
                            <Setter Property="Effect">
                                <Setter.Value>
                                    <DropShadowEffect ShadowDepth="2" BlurRadius="10" Color="Black" Opacity="0.1"/>
                                </Setter.Value>
                            </Setter>
                        </DataTrigger>
                        <DataTrigger Binding="{Binding IsSelected}" Value="True">
                            <Setter Property="Effect">
                                <Setter.Value>
                                    <DropShadowEffect ShadowDepth="0" BlurRadius="10" Color="{StaticResource PrimaryColor}" Opacity="0.6"/>
                                </Setter.Value>
                            </Setter>
                            <Setter Property="BorderBrush" Value="{StaticResource PrimaryBrush}"/>
                        </DataTrigger>
                    </Style.Triggers>
                </Style>
            </Border.Style>

            <Border.Effect>
                <DropShadowEffect ShadowDepth="1" Color="LightGray" Opacity="0.2" BlurRadius="5"/>
            </Border.Effect>

            <Grid Margin="15,10">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <!-- Аватарка -->
                    <ColumnDefinition Width="*"/>
                    <!-- Инфо -->
                    <ColumnDefinition Width="Auto"/>
                    <!-- Кол-во заказов -->
                </Grid.ColumnDefinitions>

                <!-- Кружок с инициалами -->
                <Border Grid.Column="0" Width="50" Height="50" CornerRadius="25" Background="#F5F5F5" Margin="0,0,15,0" VerticalAlignment="Center">
                    <!-- ИЗМЕНЕНИЕ ЗДЕСЬ: Binding Initials -->
                    <TextBlock Text="{Binding Initials}" 
                               FontSize="22" FontWeight="Bold" Foreground="{StaticResource PrimaryBrush}"
                               HorizontalAlignment="Center" VerticalAlignment="Center"/>
                </Border>

                <!-- Информация о клиенте -->
                <StackPanel Grid.Column="1" VerticalAlignment="Center">
                    <TextBlock Text="{Binding FullName}" FontWeight="Bold" FontSize="16" Foreground="{StaticResource DarkBackgroundBrush}" TextTrimming="CharacterEllipsis"/>

                    <StackPanel Orientation="Horizontal" Margin="0,4,0,0">
                        <TextBlock Text="Тел: " Foreground="Gray" FontSize="12"/>
                        <TextBlock Text="{Binding PhoneNumber}" Foreground="#555" FontSize="12" FontWeight="SemiBold"/>
                    </StackPanel>

                    <StackPanel Orientation="Horizontal" Margin="0,2,0,0">
                        <TextBlock Text="Email: " Foreground="Gray" FontSize="12"/>
                        <TextBlock Text="{Binding Email}" Foreground="#555" FontSize="12" TextTrimming="CharacterEllipsis"/>
                    </StackPanel>
                </StackPanel>

                <!-- Бейдж с количеством заказов -->
                <Border Grid.Column="2" Background="{StaticResource LightBackgroundBrush}" CornerRadius="15" Padding="10,5" VerticalAlignment="Center">
                    <StackPanel Orientation="Vertical" VerticalAlignment="Center">
                        <TextBlock Text="{Binding OrdersCount}" 
                                   FontSize="18" FontWeight="Bold" 
                                   Foreground="{StaticResource PrimaryBrush}" 
                                   HorizontalAlignment="Center"/>
                        <TextBlock Text="заказов" FontSize="10" Foreground="Gray" HorizontalAlignment="Center" Margin="0,-2,0,0"/>
                    </StackPanel>
                </Border>
            </Grid>
        </Border>
    </Button>
</UserControl>