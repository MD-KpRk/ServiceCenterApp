<UserControl x:Class="ServiceCenterApp.Controls.UsedServiceControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             Language="be-BY"
             d:DesignHeight="50" d:DesignWidth="800">

    <Border BorderBrush="#E0E0E0" BorderThickness="0,0,0,1" Padding="10,8">
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="2*"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="2*"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="30"/>
            </Grid.ColumnDefinitions>

            <StackPanel Grid.Column="0" VerticalAlignment="Center">
                <TextBlock Text="{Binding Name}" FontWeight="SemiBold" TextTrimming="CharacterEllipsis" TextWrapping="Wrap"/>
            </StackPanel>

            <TextBlock Grid.Column="3" Text="{Binding Price, StringFormat={}{0:C2}}" 
                       VerticalAlignment="Center" HorizontalAlignment="Right" Margin="10,0"/>

            <TextBlock Grid.Column="4" Margin="0 0 20 0" Text="{Binding TotalSum, StringFormat={}{0:C2}}" 
                       FontWeight="Bold" VerticalAlignment="Center" HorizontalAlignment="Right"/>

            <Button Grid.Column="5" Content="X" 
                    Command="{Binding RemoveCommand}" 
                    Style="{StaticResource SecondaryButtonStyle}" 
                    Padding="0" Height="30" Width="30" 
                    Foreground="Red" Margin="0,0,0,0"
                    ToolTip="Удалить услугу"/>
        </Grid>
    </Border>
</UserControl>