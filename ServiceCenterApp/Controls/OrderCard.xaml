<UserControl x:Class="ServiceCenterApp.Controls.OrderCard"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:vm="clr-namespace:ServiceCenterApp.ViewModels"
             mc:Ignorable="d" 
             d:DesignHeight="130" d:DesignWidth="450">

    <Button Command="{Binding DataContext.OpenOrderDetailsCommand, RelativeSource={RelativeSource AncestorType=Page}}"
            CommandParameter="{Binding OrderId}"
            Style="{StaticResource TransparentButtonStyle}">
        <Border Style="{StaticResource OrderBorderStyle}" Background="White" CornerRadius="5" Margin="5"
                BorderThickness="1" BorderBrush="#E0E0E0" 
                Width="450" Height="130">
            <Border.Effect>
                <DropShadowEffect ShadowDepth="1" Color="LightGray" Opacity="0.2" BlurRadius="5"/>
            </Border.Effect>
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="10"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <Border Background="{Binding StatusColor}" CornerRadius="5,0,0,5" />

                <Grid Grid.Column="1" Margin="15,10">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>

                    <DockPanel Grid.Row="0" LastChildFill="False">
                        <TextBlock FontWeight="Bold" FontSize="16" Foreground="{StaticResource DarkBackgroundBrush}">
                            <Run Text="Заказ №"/>
                            <Run Text="{Binding OrderId}"/>
                        </TextBlock>
                        <TextBlock Text="{Binding RegistrationDate}" Foreground="Gray" VerticalAlignment="Center" Margin="10,0"/>
                        <Border DockPanel.Dock="Right" Background="{Binding StatusColor}" CornerRadius="10" Padding="8,3">
                            <TextBlock Text="{Binding StatusName}" Foreground="White" FontSize="11" FontWeight="SemiBold"/>
                        </Border>
                    </DockPanel>

                    <StackPanel Orientation="Horizontal" Grid.Row="1" Margin="0,5,0,5" VerticalAlignment="Center">
                        
                        <StackPanel Width="180">
                            <TextBlock Text="УСТРОЙСТВО" FontSize="11" Foreground="Gray"/>
                            <TextBlock Text="{Binding DeviceDescription}" FontSize="14" FontWeight="SemiBold" TextTrimming="CharacterEllipsis"/>
                        </StackPanel>

                        <StackPanel Margin="6 0 0 0" Width="100">
                            <TextBlock Text="КЛИЕНТ" FontSize="11" Foreground="Gray"/>
                            <TextBlock Text="{Binding ClientFullName}" FontSize="14" FontWeight="SemiBold" TextTrimming="CharacterEllipsis"/>
                        </StackPanel>

                        <StackPanel Margin="6 0 0 0" Width="100">
                            <TextBlock Text="МАСТЕР" FontSize="11" Foreground="Gray"/>
                            <TextBlock Text="{Binding MasterFullName}" FontSize="14" FontWeight="SemiBold" TextTrimming="CharacterEllipsis"/>
                        </StackPanel>
                    </StackPanel>

                    <StackPanel Grid.Row="2">
                        <TextBlock Text="Проблема:" FontSize="11" Foreground="Gray"/>
                        <TextBlock Text="{Binding ProblemDescription}" TextTrimming="CharacterEllipsis" Foreground="#555"
                                   TextWrapping="NoWrap"/>
                    </StackPanel>
                    
                </Grid>
            </Grid>
        </Border>
    </Button>
</UserControl>