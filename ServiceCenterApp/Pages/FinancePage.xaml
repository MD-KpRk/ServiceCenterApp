<Page x:Class="ServiceCenterApp.Pages.FinancePage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      mc:Ignorable="d" 
      Language="be-By"
      d:DesignHeight="800" d:DesignWidth="1200"
      Title="Финансы">

    <Page.Resources>
        <!-- Стили для карточек KPI -->
        <Style x:Key="FinanceCardStyle" TargetType="Border">
            <Setter Property="Background" Value="White"/>
            <Setter Property="CornerRadius" Value="10"/>
            <Setter Property="Padding" Value="20"/>
            <Setter Property="Margin" Value="0,0,20,0"/>
            <Setter Property="Effect">
                <Setter.Value>
                    <DropShadowEffect ShadowDepth="2" BlurRadius="10" Opacity="0.1"/>
                </Setter.Value>
            </Setter>
        </Style>
    </Page.Resources>

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Text="Финансовый отчет" FontSize="28" FontWeight="Bold" Foreground="{StaticResource DarkBackgroundBrush}" Margin="0,0,0,20"/>

        <!-- 1. KPI КАРТОЧКИ -->
        <WrapPanel Grid.Row="1" Margin="0,0,0,20" HorizontalAlignment="Center">
            <!-- Баланс -->
            <Border Style="{StaticResource FinanceCardStyle}" Width="250" Margin="10">
                <StackPanel>
                    <TextBlock Text="Текущий баланс" Foreground="Gray" FontSize="14"/>
                    <TextBlock Text="{Binding CurrentBalance, StringFormat={}{0:C2}}" FontSize="28" FontWeight="Bold" Foreground="{StaticResource PrimaryBrush}"/>
                </StackPanel>
            </Border>

            <!-- Приход за месяц -->
            <Border Style="{StaticResource FinanceCardStyle}" Width="250" Margin="10">
                <StackPanel>
                    <TextBlock Text="Приход (Месяц)" Foreground="Gray" FontSize="14"/>
                    <TextBlock Text="{Binding IncomeThisMonth, StringFormat={}{0:C2}}" FontSize="28" FontWeight="Bold" Foreground="#2E7D32"/>
                    <!-- Зеленый -->
                </StackPanel>
            </Border>

            <!-- Расход за месяц -->
            <Border Style="{StaticResource FinanceCardStyle}" Width="250" Margin="10">
                <StackPanel>
                    <TextBlock Text="Расход (Месяц)" Foreground="Gray" FontSize="14"/>
                    <TextBlock Text="{Binding ExpenseThisMonth, StringFormat={}{0:C2}}" FontSize="28" FontWeight="Bold" Foreground="#D32F2F"/>
                    <!-- Красный -->
                </StackPanel>
            </Border>
        </WrapPanel>

        <!-- 2. ТАБЛИЦА И УПРАВЛЕНИЕ -->
        <DockPanel Grid.Row="2">
            <StackPanel DockPanel.Dock="Top" Orientation="Horizontal" Margin="0,0,0,10">
                <Button Content="- Внести Расход" Command="{Binding AddExpenseCommand}" 
                        Style="{StaticResource AccentButtonStyle}" Background="#D32F2F" Margin="0,0,10,0"/>
                <!-- Кнопку "Внести Доход" можно добавить позже, если нужно -->
            </StackPanel>

            <DataGrid ItemsSource="{Binding Transactions}"
                      AutoGenerateColumns="False" IsReadOnly="True"
                      Style="{StaticResource ModernDataGridStyle}"
                      ColumnHeaderStyle="{StaticResource ModernDataGridColumnHeaderStyle}"
                      CellStyle="{StaticResource ModernDataGridCellStyle}">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="Дата" Binding="{Binding Date, StringFormat='dd.MM.yyyy HH:mm'}" Width="140"/>

                    <DataGridTemplateColumn Header="Тип" Width="80">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <TextBlock Text="{Binding Type}" FontWeight="Bold">
                                    <TextBlock.Style>
                                        <Style TargetType="TextBlock">
                                            <Style.Triggers>
                                                <DataTrigger Binding="{Binding Type}" Value="Income">
                                                    <Setter Property="Foreground" Value="#2E7D32"/>
                                                    <Setter Property="Text" Value="Приход"/>
                                                </DataTrigger>
                                                <DataTrigger Binding="{Binding Type}" Value="Expense">
                                                    <Setter Property="Foreground" Value="#D32F2F"/>
                                                    <Setter Property="Text" Value="Расход"/>
                                                </DataTrigger>
                                            </Style.Triggers>
                                        </Style>
                                    </TextBlock.Style>
                                </TextBlock>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>

                    <DataGridTextColumn Header="Категория" Binding="{Binding Category.Name}" Width="150"/>
                    <DataGridTextColumn Header="Описание" Binding="{Binding Description}" Width="*"/>
                    <DataGridTextColumn Header="Сумма" Binding="{Binding Amount, StringFormat={}{0:C2}}" Width="100" FontWeight="Bold"/>
                    <DataGridTextColumn Header="Оплата" Binding="{Binding PaymentMethod}" Width="100"/>
                </DataGrid.Columns>
            </DataGrid>
        </DockPanel>
    </Grid>
</Page>